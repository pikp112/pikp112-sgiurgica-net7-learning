	//Data structures homework
	2.	int[,] biArray = new int[4, 3] { {123, 345, 567 },
                                            { 12,  56,  -1},
                                            { 34,  -98, 65 },
                                            { 765, 543, 321}};
            Console.WriteLine(biArray[1, 1]);
            Console.WriteLine(biArray[2, 1]);

	3.  Console.WriteLine();
            List<string> names = new List<string>();
            Console.WriteLine("Verificati daca un nume este in lista \"names\": ");
            string namePerson = Console.ReadLine();
            if (names.Contains(namePerson))
            {
                Console.WriteLine("Numele este in lista");
            }
            else
            {
                Console.WriteLine("Numele va fi adaugat in lista");
			    names.Add(namePerson);
                Console.WriteLine("Noua lista va fi: ");
                foreach (string name in names)
                {
                    Console.WriteLine(name);
                }
            }
            Console.WriteLine("Noua lista contine {0} itemi.", names.Count);
	
	4. Console.WriteLine();
            Stack<int> myStack = new Stack<int>(2);
            Console.WriteLine("Introduceti primul numar din stiva: ");
            int num1 = Convert.ToInt32(Console.ReadLine());
            myStack.Push(num1);
            Console.WriteLine("Introduceti al doilea numar din stiva: ");
            int num2 = Convert.ToInt32(Console.ReadLine());
            myStack.Push(num2);
            int sum = myStack.ElementAt(0) + myStack.ElementAt(1);
            if (myStack.Contains(30) || sum == 30)
            {
                Console.WriteLine("true");
            }
            else
            {
                Console.WriteLine("false");
            }
            Console.WriteLine("Lista dumneavoastra este: ");
            foreach(int i in myStack)
            {
                Console.WriteLine(i);
            }
	
	5.Console.WriteLine();
	  Queue<int> myQueue=new Queue<int>(5);
 	  Console.WriteLine("Introduceti primul numar din coada: ");
	  int number1=Convert.ToInt32(Console.ReadLine());
	  myQueue.Enqueue(number1);
 	  Console.WriteLine("Introduceti al doilea numar din coada: ");
	  int number2=Convert.ToInt32(Console.ReadLine());
	  myQueue.Enqueue(number2);
 	  Console.WriteLine("Introduceti al treilea numar din coada: ");
	  int number3=Convert.ToInt32(Console.ReadLine());
	  myQueue.Enqueue(number3);
 	  Console.WriteLine("Introduceti al patrulea numar din coada: ");
	  int number4=Convert.ToInt32(Console.ReadLine());
	  myQueue.Enqueue(number4);
 	  Console.WriteLine("Introduceti al cincilea numar din coada: ");
	  int number5=Convert.ToInt32(Console.ReadLine());
	  myQueue.Enqueue(number5);
	  int topNumber=myQueue.Dequeue();
	  if(topNumber>0 && topNumber%3!=0 || topNumber%7!=0){
		Console.WriteLine("Top number from myQueue is a positive number and a multiple of 3 or 7.");
		}
		else{
		myQueue.Dequeue();
		Console.WriteLine("Insert a new top numver in myQueueu: ");
		int newTopNumber=Convert.ToInt32(Console.ReadLine());
		newTopNumber=myQueue.ElementAt(0);
		}
	  Console.WriteLine("Total elements: {0}", myQueue.Count);
	  while (myQueue.Count > 0){
    			Console.WriteLine(myQueue.Dequeue());}	
	
